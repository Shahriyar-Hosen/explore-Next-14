import { NextPage } from "next";

// Make sure all content are same as page.tsx
const DefaultComplexDashboard: NextPage = () => {
  return (
    <div>
      <h1>Default Complex Dashboard</h1>
    </div>
  );
};

export default DefaultComplexDashboard;
