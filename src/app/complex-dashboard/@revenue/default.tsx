import Card from "@/components/Card";
import { NextPage } from "next";

// Make sure all content are same as page.tsx
const DefaultRevenue: NextPage = () => {
  return (
    <Card>
      <h1>Default Revenue Metrics</h1>
    </Card>
  );
};

export default DefaultRevenue;
