import Card from "@/components/Card";
import { NextPage } from "next";

// Make sure all content are same as page.tsx
const DefaultUserAnalytics: NextPage = () => {
  return (
    <Card>
      <h1>Default User Analytics</h1>
    </Card>
  );
};

export default DefaultUserAnalytics;
